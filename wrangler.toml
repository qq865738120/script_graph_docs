name = "script-graph-docs"
compatibility_date = "2024-01-01"

# Cloudflare Pages 配置
[site]
bucket = "."

# 构建配置（Docsify 不需要构建）
[build]
command = ""
cwd = ""
watch_dirs = []

# 环境变量（如果需要）
[env.production]
# 添加生产环境变量

[env.preview]
# 添加预览环境变量

